Create key pair
User enters group name as group_hash@relay1@relay2@relay3
Request number of events for the group from each relay
Download all events from the relay with highest event count
All events are in a DAG. If there are no gaps, sync is complete.
If there is a gap, request the missing event hash. Continue this process until the gap is filled.

